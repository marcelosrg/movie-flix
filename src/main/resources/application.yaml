spring:
  application:
    name: movieflix
  datasource:
    url: jdbc:postgresql://${HOST}:${DBPORT}/${DBNAME}
    username: ${DB_USER}
    password: ${DB_PASS}
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

  flyway:
    enabled: true
springdoc:
  api-docs:
    path: /api/api-docs
  swagger-ui:
    path: /swagger/index.html
movieflix:
  security:
    secret: ${SECRET:MDKANDABNDU141B4LKB1K4B1K}

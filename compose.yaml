services:
  postgres:
    image: postgres:16
    container_name: movieflix
    environment:
      POSTGRES_DB: ${DBNAME}
      POSTGRES_PASSWORD: ${DB_PASS}
      POSTGRES_USER: ${DB_USER}
    ports:
      - '${DBPORT}:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
    name: movieflix_postgres_volume